#! /usr/bin/env node

// Make sure we think we're in the right directory.
process.chdir(__dirname.replace(/bin$/,"lib"));

// Start
require('../lib/skeleton');

// Wait indefinitely. This is needed because nothing we've done yet is blocking! Neat!
process.stdin.resume();
// Test the server by running this and visiting localhost:8080 in your browser.